import styled from "styled-components";
import {
  TileContainer,
  TileTitleSection,
  TileSubTitleSection
} from "../Shared";

export const PostContainer = styled(TileContainer)``;
export const PostTitleSection = styled(TileTitleSection)``;
export const PostCommentSection = styled(TileSubTitleSection)``;
